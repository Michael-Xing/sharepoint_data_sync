apiVersion: v1
kind: Service
metadata:
  name: sharepoint-sync-service
  namespace: default
  labels:
    app: sharepoint-sync
spec:
  selector:
    app: sharepoint-sync
  ports:
  - name: health
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP


