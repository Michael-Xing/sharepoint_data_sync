apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sharepoint-sync-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi  # Adjust based on your needs
  storageClassName: standard  # Adjust based on your cluster configuration


